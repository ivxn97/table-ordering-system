<!DOCTYPE html>
<html>
<meta charset="UTF-8">
<title>CHECKOUT SUCCESSFUL</title>
<meta name="viewport" content="width=device-width,initial-scale=1">
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

<head>
    <style>
        h1{
          margin: 18px;
        }
  
        form[id = tableNo]{
          margin: 14px;
          padding: 8px;
          line-height: 1;
      }
  
        body{
          background-image: url("/Images/background4.jpg");
        }
  
        .button {
          height: 50px;
          width: 100px;
          align-items: center;
          justify-content: center;
          display: flex;
          margin: 20px;
          padding: 10px;
          border-radius: 12px;
          text-align: center;
       }
  
       input[type=text] {
          border:none;
          outline:none;
          background:transparent;
      }
  
      input[type=number] {
          border:none;
          outline:none;
          background:transparent;
      }
  
      input[type=string] {
          border:none;
          outline:none;
          background:transparent;
          font-weight: bold;
          padding-left: 2px;
      }
  
      input[id=quantity] {
        padding-left: 20px;
        text-align: left;
      }
  
      input[id=price] {
      display: inline-block;
      text-align: left;
      }
  
      input[id=priceHeader]{
        padding-left: 6px;
      }
  
      </style>
</head>

<body>
    <h1>CHECKOUT SUCCESSFUL</h1>
    <h2>Receipt:</h2><br/>
    &emsp;&ensp;&nbsp;<input type="string" name="nameHeader" id="nameHeader" value="Item Name"readonly />
    <input type="string" name="quantityHeader" id="quantityHeader" value="Quantity"readonly />
    <input type="string" name="priceHeader" id="priceHeader" value="Price"readonly />

    <ol>
      <% var count = 0; %>
      <% var total = 0; %>
      <% cart.forEach(function(carts) { %>
        <table style= "float:left">
          <tr>
          <td>
            <li><input type="text" name="name" id="name" value="<%= carts.item_name %>"readonly />
            <input type="number" name="quantity" id="quantity" value="<%= carts.quantity %>"readonly />
            <% var subtotal = carts.quantity * carts.price; %>
            <input type="number" name="price" id="price" value="<%= subtotal %>"readonly />
            </li>
          </td>
        </tr>
      </table>
      <% if (count == 0) {
        total = subtotal;
      } 
      else {
        total = total + subtotal;
      } %>
      <% count = count + 1 %>
      <br/><br/>
        <% }); %>
      <b style="padding-left: 430px;">Total: $</b><%= total %>
    </ol>
    <br/>
    <form id="backForm" action="/menuBack" method="get">
        <button type="submit" class="w3-left w3-large menuButton" margin="20px";>Back to Menu</button>
      </form>
    <script>
    </script>
</body>
</html>